<html>
    <head>
        <title>Index Stats / {{ns}}</title>
    </head>
    <body>
        <h1>Detail: {{ns}}</h1>
        <h4>{{nreturned}} / {{docsExamined}}</h4>
        <hr />
        <pre>{{raw}}</p>
    </body>
</html>