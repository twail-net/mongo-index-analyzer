<html>
    <head>
        <title>Index Stats</title>
    </head>
    <body>
        <h1>Index Stats</h1>
        <ul>
            {{#queries}}
            <li>
                <a href="{{ts}}.html">{{query.find}}</a> ({{nreturned}} / {{docsExamined}})
            </li>
            {{/queries}}
        </ul>
    </body>
</html>